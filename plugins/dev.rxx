fn init(session_in, renderer_in) {
    let session = session_in;
    let renderer = renderer_in;
    register_command("hello", "Says hello");
    print(`dev.rxx initialized`);
}

fn cmd_hello(args) {
    session.switch_mode(script_mode("hello"));
}